/* eslint-disable no-magic-numbers */
export const LONG_DURATION = 10 * 1000;

export enum FORM_CONTROL {
  amount = 'amount',
  asset = 'asset',
  deposit = 'deposit',
  recipient = 'recipient',
  transfer = 'transfer',
}
